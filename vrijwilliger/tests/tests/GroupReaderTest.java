package tests;

public class GroupReaderTest {
    //@Test
    /*public void testReadStudents() {
        FileManager gr = new FileManager();
        List<Student> list;

        try {
            Method m = FileManager.class.getDeclaredMethod("readStudents", Student.class);
            m.setAccessible(true);
            Student test = (Student)m.invoke(gr);
            list = (List<Student>)m.invoke(gr);
        } catch(NoSuchMethodException | IllegalAccessException | InvocationTargetException e) {

        }

        list = gr.readStudents();

        assertEquals(2, list.size());
    }*/
}